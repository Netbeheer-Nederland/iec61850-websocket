# Test Certificates

This directory is intentionally empty in the repository.

You must generate local TLS certificates for running tests that require SSL/TLS.

## Quick start

```bash
./generate.sh
```
