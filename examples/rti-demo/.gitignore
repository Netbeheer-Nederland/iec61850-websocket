# RTI Demo - Local configuration and generated files
# (repo-wide ignores - __pycache__, .venv, IDE files, .env, .pytest_cache,
# build/dist/*.egg-info, *.log, etc. - live in the root .gitignore and
# already apply recursively here; only rti-demo-specific entries below.)

# Local configuration files
launch_config.json
*.local.json

# Generated files
*.pid

# Docker
.docker/

# Service state files
modules/bff/src/bff/connections.json
stats.json

# Temporary files
*.tmp
*.bak

# Local overrides
launch.py.local
config.local.py
